'use strict';

/**
 * Describes the configuration values to use.
 */
export interface IConfiguration {
    loadUnitTests: boolean;
    loadDocumentationTests: boolean;
    loadIntegrationTests: boolean;
}
